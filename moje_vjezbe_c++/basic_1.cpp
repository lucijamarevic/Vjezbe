#include <iostream>
using namespace std;

int main() {
    char a;    // Defining the type of a variable
    int num;

    cout << "Enter a character and an integer: ";
    cin >> a >> num;     // Taking input

    cout << "Character: " << a << endl;
    cout << "Number: " << num;

    return 0;
}